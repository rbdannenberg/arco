# alpass.srp -- feedback all-pass filter unit generator
#
# Roger B. Dannenberg
# June 2023

class Alpass (Ugen):
    def init(chans, inp, dur, fb, maxdur):
        super.init(create_ugen_id(), "Alpass", chans, 'a', "UUUf",
	           'inp', inp, 'dur', dur, 'fb', fb, 'maxdur', maxdur)


def alpass(inp, dur, fb, maxdur, optional chans = 1):
    Alpass(chans, inp, dur, fb, maxdur)  # alpass as a function


