# monodistortion.srp - constructor implementation
#
# (machine generated by u2f.py)

def monodistortion(input, gain, tone, volume, optional chans):
    if not chans:
        chans = max_chans(max_chans(max_chans(max_chans(1, input), gain), tone), volume)
    Ugen(create_ugen_id(), "monodistortion", chans, 'a', "UUUU", 'input',
         input, "a", 'gain', gain, "bc", 'tone', tone, "bc", 'volume', volume, "bc")

